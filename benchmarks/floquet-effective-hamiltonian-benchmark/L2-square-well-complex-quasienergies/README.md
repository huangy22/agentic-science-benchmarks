# L2 Square-Well Complex Quasienergies

This runnable L2 task packages the accepted reference
`flq_l2_driven_square_well_quasienergies` from
`../data/floquet_reference.csv`.

The source values are backed by both the LKM graph node
`paper:812308293425823745::conclusion_1` and a LKM
`papers/content/batch` markdown check recorded in
`../data/original_paper_checks.csv`.

Run:

```bash
bash scripts/selfcheck.sh
```
