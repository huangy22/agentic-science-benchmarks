# L3 Graphene Antidot Critical Amplitudes

This runnable L3 task packages the accepted reference
`flq_l3_graphene_antidot_critical_amplitudes` from
`../data/floquet_reference.csv`.

Gold values:

- `E0c1 = 2.15` a.u.
- `E0c2 = 4.62` a.u.

These are backed by both the LKM source node
`paper:867767689039315299::conclusion_2` and the original arXiv source check
recorded in `../data/original_paper_checks.csv`.

Run:

```bash
bash scripts/selfcheck.sh
```
