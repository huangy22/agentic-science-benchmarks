# L3 Graphene Antidot Photon Windows

This runnable L3 task packages the accepted reference
`flq_l3_graphene_antidot_photon_windows` from
`../data/floquet_reference.csv`.

Gold windows are backed by both the LKM source node
`paper:867767689039315299::conclusion_8` and the original arXiv source check
recorded in `../data/original_paper_checks.csv`.

Run:

```bash
bash scripts/selfcheck.sh
```
