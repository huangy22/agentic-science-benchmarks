# L3 PT-BBH Mode Counts

This runnable L3 task packages the accepted reference
`flq_l3_pt_bbh_indices_same_gap` from `../data/floquet_reference.csv`.

The hidden target is backed by the LKM graph node
`paper:1121428643831087110::conclusion_5` and a Bohrium LKM
`papers/content/batch` markdown check recorded in
`../data/original_paper_checks.csv`.

Run:

```bash
bash scripts/selfcheck.sh
```
